version: "3.1"

services:
  bolt:
    image: lukechannings/deno:v1.39.4
    volumes:
      - .:/app
    command: run -A /app/app/index.js
    network_mode: host
    working_dir: /app
    restart: always
